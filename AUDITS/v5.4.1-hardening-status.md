# v5.4.1 hardening status

Status: RELEASED - REGRESSION VERIFIED.

v5.4.1 is a deterministic hardening patch release based on the v5.4 Round 3 audit corpus. It does not expand the policy architecture.

Closed findings include the two invalid YAML test assumptions reproduced independently by Big Pickle and DeepSeek V4 Flash; empty and whitespace-only verifier match terms; synthetic stdout/stderr boundary matches; missing scope on `NOT_FOUND_WITHIN_SCOPE`; obvious same-source/same-identity reuse across declared independent T3 groups; schema-form drift; whitespace-only load-bearing evidence strings; under-constrained `PARTIAL`; required-reference symlink escape; UTF-8 BOM alignment; `CLEAN` versus `CLEAN_OBSERVED` drift; current research-title drift; expanded liveness self-tests; bounded in-memory command-output handling; and quoted duplicate top-level YAML keys.

Installation documentation also requires an explicit startup integration step for always-on use: workspaces using `/start` or an equivalent boot procedure must load `anti-hallucination-protocol` at the beginning of every Hermes session. File installation alone is not treated as session activation.

Final release verification ran on GitHub Actions with Python 3.11 and 3.13. Both matrix jobs completed successfully. The Python 3.13 job collected 124 tests and reported `124 passed`, followed by `V5 INTEGRITY: PASS`, `RESEARCH PROVENANCE: PASS`, and portable L1/L2 liveness PASS. The Python 3.11 matrix job also completed all four gates successfully.

Behavioral obedience remains unproven and is not claimed. The deterministic helpers are explicit tools, not automatic Hermes runtime enforcement.
