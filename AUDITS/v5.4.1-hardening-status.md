# v5.4.1 hardening status

Status: FINAL RELEASE VERIFICATION RUNNING.

v5.4.1 is a deterministic hardening patch release based on the v5.4 Round 3 audit corpus. It does not expand the policy architecture.

Closed findings include the two invalid YAML test assumptions reproduced independently by Big Pickle and DeepSeek V4 Flash; empty and whitespace-only verifier match terms; synthetic stdout/stderr boundary matches; missing scope on `NOT_FOUND_WITHIN_SCOPE`; obvious same-source/same-identity reuse across declared independent T3 groups; schema-form drift; whitespace-only load-bearing evidence strings; under-constrained `PARTIAL`; required-reference symlink escape; UTF-8 BOM alignment; `CLEAN` versus `CLEAN_OBSERVED` drift; current research-title drift; expanded liveness self-tests; and quoted duplicate top-level YAML keys.

Installation documentation now also requires an explicit startup integration step for always-on use: workspaces using `/start` or an equivalent boot procedure must load `anti-hallucination-protocol` at the beginning of every Hermes session. File installation alone is not treated as session activation.

The previous full release-candidate gate passed on Python 3.11 and 3.13. The Python 3.13 job collected 123 tests and reported 123 passed, with integrity, offline research provenance, and portable liveness all passing. This final verification run exists to include the last post-merge duplicate-key hardening in the tested release snapshot.

Behavioral obedience remains unproven and is not claimed. The deterministic helpers are explicit tools, not automatic Hermes runtime enforcement.
